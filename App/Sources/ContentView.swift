import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Hello, macOS 🌊")
                .font(.title)
        }
        .padding()
    }
}
